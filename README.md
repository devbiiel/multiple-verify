A simple program that calculates if one number is a multiple of another in JavaScript.

The following was used in the project:

- Node.js 
- Git bash

The program runs in the bash terminal, which can be initialized as:

node src/index.js
or 
npm run start

This was my first JavaScript project.